name: Deploy Majick Studies to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'pages-release/majick-pages-site.zip'
      - '.github/workflows/deploy-majick-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: majick-pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify Majick Studies package
        shell: bash
        run: |
          set -euo pipefail
          test -f pages-release/majick-pages-site.zip

      - name: Assemble Majick Studies site
        shell: bash
        run: |
          set -euo pipefail
          rm -rf _site
          mkdir -p _site
          unzip -oq pages-release/majick-pages-site.zip -d _site
          touch _site/.nojekyll
          test -f _site/index.html
          test -f _site/sanctuary/index.html
          echo "Majick Studies V3.3.8 assembled successfully."

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
